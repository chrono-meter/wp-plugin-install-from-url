{
    "scripts": {
        "update-third-party": "COMPOSER=third-party.json composer update",
        "zip": "npx --yes @chrono-meter/wp-zip-package --source-directory=./ --output=./release/"
    }
}
