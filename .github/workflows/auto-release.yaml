on:
  push:
    tags:
      - 'v*'

jobs:
  main:
    uses: chrono-meter/actions-wordpress/.github/workflows/ci-plugin.yaml@main
    permissions:
      contents: write
      actions: write
    with:
      enable-tests: false
